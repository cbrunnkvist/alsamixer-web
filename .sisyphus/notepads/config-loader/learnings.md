# Learnings
- Implemented a simple configuration loader for Go projects using defaults, environment variables, and CLI flags with proper precedence.
- Added unit tests to validate defaults, env overrides, and CLI overrides.
- Created a help text generator for --help/-h compatibility.
